# Reference material

| File | Status |
|---|---|
| `hex-rpg-rulebook.md` | **present** — the authoritative rules document |
| `webapp-spec.md` | present — the build plan |
| `hex-rpg-cards.html` | missing — the printed event and item cards |
| `hex-rpg-tiles.html` | missing — the tile artwork v0.1 was meant to port |
| `hex-rpg-tokens.html` | missing |

The rulebook arrived at v0.7 and the game was aligned to it; see the README's
"What the rulebook changed" table for what moved.

The three HTML files are still missing. Nothing is blocked on them: the tile art is
original SVG in `src/ui/Tile.tsx`, and the card text comes from rulebook §13. If they
turn up, the artwork is the part worth porting.
